# Sanitized Example Inputs

These are fictitious examples for documentation only. They are not production parameters and not a recommended preset.

```text
InpAllowedSymbol = XAUUSD
InpEnableLiveOrders = false
InpEnableTesterOrders = true
InpMagicNumber = 26060201

SpreadMax_points = example value only
SlippageMax_points = example value only
risk_base_pct = small research value only
max_daily_loss_R = example value only
max_trades_day = example value only

use_session_filter = true
trade_hour_mask = empty or fictitious hours
trade_weekday_mask = empty or fictitious weekdays

use_equity_risk_guard = false
use_equity_dd_hard_stop = false
use_macro_regime_filter = false
```

Do not infer private or production presets from this file. Real presets are intentionally excluded from the public repository.
